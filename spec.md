# 강사 메일 답장 도우미 — 기획서

만들려는 것 : 강사님을 고르면 그분과 주고받은 메일이 말풍선으로 보이고,
그 자리에서 답장 초안까지 만들어주는 화면

---

## 목표 — 누구의 무슨 일을 줄이나

교육운영 담당자가 **강사 한 분에게 답장을 쓰기까지 걸리는 시간**을 줄인다.

지금은 답장 한 통을 쓰려면 이렇게 한다.

1. 메일함에서 그 강사님 이름으로 검색해 지난 메일을 찾는다
2. 배정 시트를 따로 열어 일정·장소·진행 상태를 확인한다
3. 상태에 맞는 문구를 매번 처음부터 쓴다

이 화면은 세 가지를 한 자리에 모아, **찾는 일과 처음부터 쓰는 일**을 없앤다.
담당자가 하는 일은 "고르고 → 초안 확인하고 → 고쳐 쓰기"만 남는다.

---

## 입력 — 무엇을 받나

받는 것은 **강사 한 명 고르기, 딱 하나**다. 담당자가 타이핑할 것은 없다.

화면이 이미 갖고 있는 값 (미리 넣어둔 가짜 예시 자료)

| 항목 | 예시 |
| --- | --- |
| 이름 | 이지현 |
| 역할 | 강사 / 보조강사 |
| 담당 모듈 | 파이썬 기초 문법 |
| 진행 상태 | 확정 / 회신 대기 / 강의안 대기 / 일정 조율 중 / 보류 |
| 일정 | 9월 15일(월) 10:00~13:00 |
| 장소 | 강남 러닝센터 3층 A강의실 |
| 주고받은 메일 | 보낸 사람, 시각, 제목, 본문 |

- 실제 메일함이나 회사 자료는 연결하지 않는다. 전부 연습용 가짜 예시다.
- 담당자가 채워 넣어야 하는 빈칸은 없다.

---

## 단계 — 어떤 순서로 처리하나

1. **고른다** — 왼쪽 명단에서 강사님 한 분을 누른다
2. **모은다** — 그분의 이름·모듈·일정·장소·진행 상태를 한 묶음으로 가져온다
3. **보여준다** — 그분과 주고받은 메일을 시간 순서대로 말풍선으로 펼친다
   (우리가 보낸 메일은 오른쪽, 강사님이 보낸 메일은 왼쪽)
4. **고른다(자동)** — 진행 상태에 맞는 답장 틀을 하나 고른다
   - 확정 → 확정 안내와 당일 준비 안내
   - 회신 대기 → 정중한 재확인
   - 강의안 대기 → 강의안 전달 요청
   - 일정 조율 중 → 대체 일정 제안
   - 보류 → 사정 이해와 다음 기수 안내
5. **채운다** — 그 틀의 빈칸에 이름·모듈·일정·장소를 넣어 문장을 완성한다
6. **내놓는다** — 완성된 초안을 아래 칸에 띄운다. 담당자가 바로 고쳐 쓸 수 있다

**버튼은 「답장 초안 만들기」 하나뿐이다.** 4~6번은 이 버튼 한 번에 모두 일어난다.

---

## 출력 — 무엇이 어떤 모양으로 나오나

화면 한 장에 **덩어리 네 개**가 보인다. 담당자가 가져가는 결과물은 그중 ④ 하나뿐이다.

| | 덩어리 | 자리 | 몇 개 |
| --- | --- | --- | --- |
| ① | 강사 명단 | 왼쪽 | 16줄 (항상 같음) |
| ② | 고른 강사 머리글 | 오른쪽 위 | 1개 |
| ③ | 주고받은 메일 말풍선 | 가운데 | 2~3개 (상태에 따라) |
| ④ | 답장 초안 | 아래 | 1개 |

---

### ① 강사 명단 — 표, 3열 × 16줄

| 열 이름 | 들어가는 값 | 예시 |
| --- | --- | --- |
| 그림 | 픽셀 캐릭터 (사람마다 머리색·옷색이 다름) | — |
| 이름 | 강사 이름 | 이지현 |
| 담당 모듈 | 맡은 과정 이름 | 파이썬 기초 문법 |

- 줄 수는 **16줄** — 강사 8줄 + 보조강사 8줄
- 두 묶음으로 나뉘고, 묶음마다 제목 줄이 1개씩 붙는다 (`강사`, `보조강사`)
- 지금 고른 한 줄만 배경색이 들어온다

### ② 고른 강사 머리글 — 1개, 값 4가지

| 열 이름 | 예시 |
| --- | --- |
| 그림 | 픽셀 캐릭터 |
| 이름·역할 | 이지현 강사님 |
| 모듈 · 일정 | 파이썬 기초 문법 · 9월 15일(월) 10:00~13:00 |
| 상태 | 확정 (동그란 배지 1개) |

### ③ 주고받은 메일 말풍선 — 2~3개

**개수는 상태가 정한다**

| 상태 | 말풍선 개수 | 순서 |
| --- | --- | --- |
| 확정 | 3개 | 운영팀 → 강사님 → 운영팀 |
| 회신 대기 | 2개 | 운영팀 → 운영팀 |
| 강의안 대기 | 3개 | 운영팀 → 강사님 → 운영팀 |
| 일정 조율 중 | 3개 | 운영팀 → 강사님 → 운영팀 |
| 보류 | 2개 | 운영팀 → 강사님 |

**말풍선 하나 안에는 3가지가 들어간다**

| 자리 | 내용 | 예시 | 줄 수 |
| --- | --- | --- | --- |
| 윗글 | 보낸 사람 · 시각 | 교육운영팀 · 8월 25일 (화) 오전 10:12 | 1줄 |
| 제목 | 메일 제목 | [파이썬 기초 문법] 강사 배정 안내 | 1줄 |
| 본문 | 메일 내용 | 안녕하세요, 이지현 강사님… | 6~12줄 |

- 우리가 보낸 메일은 **오른쪽·연파랑**, 강사님이 보낸 메일은 **왼쪽·흰색**
- 시간이 이른 것부터 아래로 쌓인다

### ④ 답장 초안 — 글 상자 1개, 14줄

담당자가 실제로 쓰는 결과물이다. 상자는 **하나뿐이고, 나오는 글도 한 종류**다.

**줄 수 — 모두 14줄 (글자 있는 줄 9줄 + 사이 빈 줄 5줄)**

| 부분 | 줄 수 | 예시 |
| --- | --- | --- |
| 인사 | 2줄 | 안녕하세요, 이지현 강사님. / 알고링크 교육운영팀입니다. |
| 용건 | 1줄 | 파이썬 기초 문법 과정 배정이 아래와 같이 확정되었습니다. |
| 일정·장소 | 2줄 | · 일시 : 9월 15일(월) 10:00~13:00 / · 장소 : 강남 러닝센터 3층 A강의실 |
| 안내 | 2줄 | 상태에 따라 문장이 바뀌는 부분 |
| 맺음 | 2줄 | 늘 도움 주셔서 감사합니다. / 알고링크 교육운영팀 드림 |

**나오는 모양**

```
안녕하세요, 이지현 강사님.
알고링크 교육운영팀입니다.

파이썬 기초 문법 과정 배정이 아래와 같이 확정되었습니다.

· 일시 : 9월 15일(월) 10:00~13:00
· 장소 : 강남 러닝센터 3층 A강의실

당일에는 시작 30분 전까지 도착해 주시면
기자재 점검을 도와드리겠습니다.

늘 도움 주셔서 감사합니다.

알고링크 교육운영팀 드림
```

- 다섯 상태 모두 이 5부분·14줄 뼈대를 그대로 쓴다. **바뀌는 것은 「용건」과 「안내」의 문장뿐**이다
- 그대로 메일에 붙여 넣을 수 있는 글이다
- 화면 안에서 바로 고쳐 쓸 수 있다
- 파일 내려받기, 메일 자동 발송, 첨부는 없다

---

## 이 화면이 하지 않는 것

- **저장하지 않는다** — 고쳐 쓴 초안은 새로고침하면 사라진다. 쓸 내용은 담당자가 메일함으로 옮긴다
- **로그인·회원 구분이 없다** — 누구나 파일을 열면 같은 화면을 본다
- **실제 메일함에 연결하지 않는다** — 보이는 메일은 전부 연습용 가짜 예시다
- **화면은 한 장, 버튼은 하나, 결과는 한 종류다** — 다른 화면으로 넘어가지 않는다
